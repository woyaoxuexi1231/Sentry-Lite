# Release notes

## Sentry-Lite v1.4.0

仪表盘改版：按指标看健康时段，按天回看 24h，并展示网络用量。

### 下载

| 文件 | 说明 |
|------|------|
| **`Sentry-Lite-1.4.0-x64.zip`** | 解压即用（含 exe、LHM、依赖、`web/` + Inter 字体） |

### 变更

- **去掉综合 Uptime**：CPU / GPU / RAM 各自一条健康时段条（绿/黄/红），右侧为健康占比
- **时间范围**：`30m` 实时窗；`Day` 选日期看当天 24h（自定义日历 + Today 快捷返回）
- **去掉 7d** 连续区间视图
- **NET 用量**：按时段柱高展示相对流量，并汇总 ↓ / ↑ 总量（由历史速率 × 时段估算）
- 区间 max / avg 统计上移到时间线上方
- 设计稿：`design/ui-mockup-v2.html`
